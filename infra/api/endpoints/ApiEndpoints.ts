export enum ApiEndpoints {
    // PET STORE endpoints
    PET = 'pet',
    UPLOAD_IMAGE = 'uploadImage',

    // POKEMON endpoints
    POKEMON = 'pokemon',

    // GO REST API endpoints
    USERS_ENDPOINT = 'users',
}